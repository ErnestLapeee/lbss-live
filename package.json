{
  "name": "lbss",
  "version": "1.0.0",
  "private": true,
  "description": "Latvijas Beisbola Softbola Savieniba - Official Platform",
  "scripts": {
    "prepare": "node scripts/prepare-shared.cjs",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "pnpm --filter @lbss/shared exec tsc --noEmit && pnpm --filter @lbss/api exec tsc --noEmit && pnpm --filter @lbss/admin exec tsc --noEmit && pnpm --filter @lbss/web exec tsc --noEmit",
    "db:generate": "pnpm --filter @lbss/api db:generate",
    "db:migrate": "pnpm --filter @lbss/api db:migrate",
    "db:seed": "pnpm --filter @lbss/api db:seed",
    "db:studio": "pnpm --filter @lbss/api db:studio"
  },
  "packageManager": "pnpm@10.6.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "argon2",
      "esbuild",
      "sharp"
    ]
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "serve": "^14.2.5"
  }
}
